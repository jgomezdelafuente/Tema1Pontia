name: Workflow Manual
on:
  workflow_dispatch:
    inputs:
      nombre:
        description: 'Tu nombre'
        required: true
        default: 'Nombre'
      apellidos:
        description: 'Cuales son tus apellidos'
        required: true
        default: 'Apellidos'

jobs:
  saludo:
    runs-on: ubuntu-latest
    steps:
      - name: Saludo personalizado
        run: echo "Hola, ${{ github.event.inputs.nombre }}!"